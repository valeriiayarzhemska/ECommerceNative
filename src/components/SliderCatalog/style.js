import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    carouselSlider: {
      flex: 1,
      flexDirection: 'row',
      justifyContent: 'center',
      marginBottom: 20,
    },
  });
};
