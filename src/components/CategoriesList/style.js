import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    listContainer: {
      marginBottom: 18,
    },
    list: {
      width: '100%',
      height: '100%',
    },
  });
};
