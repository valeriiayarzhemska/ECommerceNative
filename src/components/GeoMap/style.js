import { StyleSheet } from 'react-native';
import { colors } from '../../constants';

export const styles = () => {
  return StyleSheet.create({
    container: {
      width: '100%',
    },
    buttonContainer: {
      width: '100%',
    },
    mapContainer: {
      marginTop: 10,
      width: '100%',
      height: 300,
    },
  });
};
