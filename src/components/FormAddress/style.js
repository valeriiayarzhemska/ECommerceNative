import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    container: {
      justifyContent: 'center',
    },
    input: {},
    button: {
      marginTop: 10,
      alignItems: 'center',
    },
  });
};
